# ─────────────────────────────────────────────────────────────
# PWA Maker Android — environment variable reference
# Copy this file to .env and fill in the values.
# Never commit .env to version control.
# ─────────────────────────────────────────────────────────────

# Host port the frontend is exposed on (change if 80 is already in use)
# To use port 80: HOST_PORT=80
HOST_PORT=8088

# Node environment (development | production)
NODE_ENV=production

# Backend listen port (internal Docker network only)
PORT=3001

# Android SDK root (set automatically in Docker; override for local dev)
ANDROID_HOME=/opt/android-sdk

# JDK home (set automatically in Docker; override for local dev)
JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64

# Gradle user home — mount as Docker volume to cache downloads
GRADLE_USER_HOME=/home/appuser/.gradle

# Maximum number of APK builds running simultaneously
MAX_CONCURRENT_BUILDS=3

# Maximum builds per IP per hour (sliding window)
BUILD_RATE_LIMIT_PER_HOUR=10

# Hours to keep a completed APK available for download before cleanup
BUILD_TTL_HOURS=1

# CORS allowed origin (set to your domain in production)
# Use * only behind a protected reverse proxy
CORS_ORIGIN=*

# Secret key for HMAC-SHA256 build tokens (anti-bot).
# Generate a strong secret: openssl rand -hex 32
# If not set, a random secret is generated at startup (tokens invalidate on each container restart).
BUILD_TOKEN_SECRET=
